module.exports = {
  root: true,
  extends: ["next/core-web-vitals"],
  // Remove problematic options that caused the ESLint error
  // The "useEslintrc" and "extensions" options are not needed
  rules: {
    // Add any custom rules here
  }
};
