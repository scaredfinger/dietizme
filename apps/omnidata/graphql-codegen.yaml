schema: ./node_modules/@otiuming/domain-omnidata-types/schema.graphql
overwrite: true
skipDocumentsValidation: true
verbose: true
documents:
  - ./sample/**/*.graphql
config:
  scalars:
    DateTime: Date
    JSON: '{ [key: string]: any }'
    numeric: number
generates:
  sample/generated.ts:
    plugins:
      - typescript
      - typescript-operations
      - typescript-graphql-request
