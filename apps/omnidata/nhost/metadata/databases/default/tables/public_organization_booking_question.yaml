table:
  name: organization_booking_question
  schema: public
object_relationships:
  - name: text
    using:
      foreign_key_constraint_on: text_id
select_permissions:
  - role: public
    permission:
      columns:
        - value
        - position
        - scope
        - type
        - id
        - organization_id
        - text_id
      filter: {}
  - role: user
    permission:
      columns:
        - id
        - type
        - scope
        - value
        - organization_id
        - text_id
        - position
      filter: {}
