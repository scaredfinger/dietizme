table:
  name: venue_usp
  schema: public
object_relationships:
  - name: headline
    using:
      foreign_key_constraint_on: headline_id
  - name: name
    using:
      foreign_key_constraint_on: name_id
  - name: venue
    using:
      foreign_key_constraint_on: venue_id
select_permissions:
  - role: public
    permission:
      columns:
        - id
        - venue_id
        - name_id
        - headline_id
        - position
      filter: {}
  - role: user
    permission:
      columns:
        - position
        - headline_id
        - id
        - name_id
        - venue_id
      filter: {}
