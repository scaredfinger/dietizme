schema: ../../schema.graphql
overwrite: true
verbose: true
documents:
  - src/graphql/**/*.graphql
config:
  scalars:
    DateTime: Date
    JSON: '{ [key: string]: any }'
generates:
  src/generated.ts:
    plugins:
      - typescript
      - typescript-operations
      - typescript-graphql-request
