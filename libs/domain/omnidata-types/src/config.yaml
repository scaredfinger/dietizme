schema: ../../../schema.graphql
overwrite: true
config:
  scalars:
    DateTime: Date
    JSON: '{ [key: string]: any }'
    numeric: number
generates:
  src/index.ts:
    plugins:
      - typescript
